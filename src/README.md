```
virtualenv venv
source venv/bin/activate
pip install openai ipython jupyter langchain
pip install langchain langchain-community bs4 sentence-transformers

#sentence-transformers faiss-cpu  langchain-groq
```
